<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AppResource.CustomControls;assembly=AppResource"
        xmlns:local="clr-namespace:CustomControlsTest" x:Class="CustomControlsTest.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Name="grid">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="pack://application:,,,/AppResource;component/CustomControls/CustomCheckBox.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.DataContext>
            <local:MainWindowVM/>
        </Grid.DataContext>
        <!--<controls:CustomListButton Width="550"  Height="200" >
        </controls:CustomListButton>-->
        <CheckBox Style="{DynamicResource SliderCheckBox}" IsChecked="{Binding WindowsAutomatically, Mode=TwoWay}" Margin="0,0,0,15" Height="20" Width="40"/>
    </Grid>
</Window>
